{
  "name": "codeforces-client",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package":"electron-packager . Codeforces --platform=win32 --arch=x64 --icon=favicon.ico --out=./dist --asar --app-version=0.5.0 --overwrite --ignore=node_modules"
  },
  "devDependencies": {
    "electron": "^8.0.1",
    "electron-packager": "^15.0.0"
  }
}
